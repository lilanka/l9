#include "compiler.h"
#include "scanner.h"

namespace L9 {

void Compiler::compile(const char* source) {
  Scanner scanner{source};
  /*
  int line = -1;
  while (true) {
    Token token = scan();  
  }
  */
}

}; // namespace L9